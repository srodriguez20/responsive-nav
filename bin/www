#!/usr/bin/env node
var app = require('../app');
var port = process.env.PORT || 3000;

app.set('port', port);

app.listen(port, function() {
  console.log('Endava Basic FE Challenge is available at http://localhost:' + port);
});
